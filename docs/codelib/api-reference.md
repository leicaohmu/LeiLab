---
icon: material/api
tags:
  - API
---

# API 参考

!!! info "使用说明"
    以下文档由 `mkdocstrings` 插件从源码注释自动生成。

## 示例：调用方式

在 Markdown 中使用如下语法即可自动渲染 API 文档：

```markdown
::: biostatai.module.ClassName
```

!!! warning "建设中"
    请将你的 Python 包路径配置到 mkdocs.yml 后，替换上方示例路径。
